{
  "name": "al-hasaniyyah",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd mobile && npx expo export -p web",
    "build:mobile": "cd mobile && npx expo export -p web",
    "build:admin": "cd admin && npm run build"
  }
}
