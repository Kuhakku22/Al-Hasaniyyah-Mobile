{
  "name": "al-hasaniyyah-admin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd admin && npm install && npm run build && node -e \"require('fs').cpSync('admin/.next', '.next', {recursive: true})\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.40.0",
    "next": "16.2.10",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  }
}
